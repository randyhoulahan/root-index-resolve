{
  "name": "origin-504-detection",
  "version": "1.0.0",
  "description": "serverless aws s3 lambda@edge detect 504 from orgin",
  "main": "index.js",
  "repository": "",
  "author": "",
  "license": "MIT",
  "private": false,
  "scripts": {
    "deploy:dev": "serverless deploy -v -s dev",
    "deploy:prod": "serverless deploy -v -s prod",
    "offline": "serverless invoke local --function main --path tests/invoke.json -s dev"
  }
}
